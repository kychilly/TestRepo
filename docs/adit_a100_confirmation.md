# Adit A100 confirmation

Use [team_week2_to_week4_execution.md](team_week2_to_week4_execution.md),
section 5. It contains the current environment pins, preflight, benchmark,
checkpoint/resume command, and acceptance criteria.

With the data currently present, expect three completed `validator_off` seed
runs and nine explicit validator-on blockers. Expect all 12 scGPT runs only
after independent protein evidence produces confirmed genes and Stage 3/4 is
rerun. A manifest reporting blockers must never be presented as a completed
12-arm scientific matrix.
